# Required for semantic search; use your own Google Cloud project.
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us-central1
# Optional service account file; otherwise use gcloud auth application-default login.
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json
